# node/pnpm は mise で管理する
# direnv は bash で評価されるため -s bash を明示する
if has mise; then
  eval "$(mise env -s bash)"
fi

# .env から TAKUMI_GUARD_TOKEN / OBSIDIAN_PLUGIN_DIR をシェル環境へ読み込む。
# .env はどちらも任意設定のため、存在しなくてもエラーにしない。
dotenv_if_exists
